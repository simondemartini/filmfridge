<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Text.Subhead" parent="Base.TextAppearance.AppCompat.Subhead">
        <item name="android:textStyle">bold</item>
        <item name="android:scrollHorizontally">false</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="AppTheme.SettingsItem">
        <item name="android:layout">@android:layout/simple_list_item_1</item>
        <item name="android:padding">@dimen/activity_horizontal_margin</item>
        <item name="android:scrollHorizontally">false</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="AppTheme.SettingsItemTitle">
        <item name="android:scrollHorizontally">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:ellipsize">end</item>
        <item name="android:layout_weight">1</item>
    </style>
    <style name="AppTheme.SettingsItemSubHead">
        <item name="android:scrollHorizontally">false</item>
        <item name="android:textStyle">italic</item>
        <item name="android:ellipsize">end</item>
        <item name="android:layout_weight">2</item>
    </style>
    <style name="AppTheme.SettingsItemContent">
        <item name="android:scrollHorizontally">false</item>
        <item name="android:ellipsize">end</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">right</item>
    </style>
    <style name="AppTheme.SettingsItemWideWidget">
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:scrollHorizontally">false</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

</resources>
